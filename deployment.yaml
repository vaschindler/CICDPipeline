apiVersion: v1
kind: Pod
metadata:
   name: node-pod
spec:
   containers:
   - name: node-pod
     image: dockerbuildapp/test
     ports:
     - containerPort: 3000        
     imagePullPolicy: Latest
     command: ["/bin/sh", "-ec", "sleep 1000"]
   imagePullSecrets:
   - name: regcred
